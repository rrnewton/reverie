Advance Hermit's complete Reverie pin to actual landed a2cc1868795b19ae77e55242d614c43116e42609 (https://github.com/rrnewton/reverie/pull/561). Root independently verified the seven blobs/modes equal reviewed066a. The new canonical source destination is worktrees/slots/dev-hermit-dbt-loader-pin-20260917, branch dev-hermit/dbt-loader-pin-20260917, from fresh Hermit main2a4142371e74aebea3cbd8b4c8329fc819d324f9. Allocation and ordinary pin preparation are released; official guest execution remains held for final source/controller review.

Product scope: eight active Cargo.toml files and two tracked Cargo.lock files (pin-sites.json), plus ci/run-with-reverie-dbt-budget.sh and ci/configure-build-jobs.sh. Preserve every dependency URL/features and all non-Reverie pins; resolve the two lockfiles with normal targeted Cargo updates and inspect any transitive resolution delta. No Rust/test/image/graph change is planned. Current LiteInst runtime cache paths derive the pin dynamically: the canonical literal-token search found zero active revision suffixes, so no extra LiteInst source edit is needed.

The two budget files contain three active exact-pin checks. Carry them together with an explicit rationale and the actual new native recipe key0aa6d84239b5a04b7cda124ebed4c7e3adc8b62f5b4c96011a9b971e90d6b0a4; do not claim native recipe identity with oldc9c. The loader C source changes, while build.rs, native build targets/options and sixteen-job clamp remain unchanged. Genuine native MISS took27.71s at jobs4 under4CPU/8GiB, about110.84 job-seconds versus the unchanged1050 threshold. This local sample supports the unchanged carry; it does not replace the hosted three-sample calibration or claim five hosted samples. Preserve1050,16 and all conversion/decline/ratchet semantics. Normal official pinned builds must still enforce them and record the actual new native source key.

The pin also carries a28 RPC transport changes. New mapped/IO-scope APIs have no current production caller, but the existing async socket codec now distinguishes partial header EOF as Io(UnexpectedEof) instead of clean closure and explicitly retries Interrupted reads. Existing DBT/LiteInst/SaBRe servers use that reader; serve logs and retires a malformed connection, while direct serve_one/serve_connection returns its error. This is real stricter framing behavior, not an API-only attribution. The native loader fixtures do not link that Rust RPC path. See rpc-attribution/REPORT.md for source/consumer details. Run the existing focused header/EOF controls under the same source-bound private Cargo setup; no new test or comparator is needed.

Current official selection is exactly test.hermit_unit, with ten required ancestors and8700 seconds summed wall budgets; selected-closure.json retains every original row field and graph resource policy. Main declares630 cases with --lib --bins and third-party-backends. This is the current declaration, not a new measured collection. The pin must preserve all630 identities/assertions and independent prepared count/source authority. Original DBT echo/true remain unchanged, including exact exit/stdout/Detcore summary assertions and original57-second case backstop. A passed test that took an existing unavailable-backend early return is not DBT execution evidence; retain prepared runtime/executable availability/source evidence and actual raw outcomes. New diagnostic629/strace/image377c from the old observer slot is not imported.

Use the committed main image244cb70374721331a92bca7f0f4f6518d9fe31e827ed3f8fc43e7a3bf99871a7; actual local image IDf92820fbd9cdcc8620931e1e80bcf1a44fcbbba2db95c82f5c76f16f363ffde7 was read without running a container. Pin preparation uses a new private Cargo/download cache and external XDG_CACHE_HOME. Prefer read-only terminal6108 registry/git seed with cp -a --reflink=auto and recursive path/type/bytes/hash plus distinct inode proof before/after. Do not pass a retained donor to a writable pinned --cargo-home mount. Do not copy the host-built candidate native install into the pinned image build cache. Target-cache reuse is optional only after source/tool/provider compatibility proof; a normal changed-source native build remains mandatory and no prepared receipt is copied as authority.

Measured donor allocated sizes: Cargo1,093,222,400B, prior pinned target74,424,492,032B, external host cache730,746,880B. At this observation the workspace filesystem used2,271,719,583,744B with1,430,479,310,848B available. These are monitoring facts, not a resurrected dropped-experiment hard quota or a claim that physical free meets the owner's disk target. No foreign deletion or donor mutation is authorized. Prefer the small input seed; report additional actual output growth and complete genuine prerequisites without budget weakening.

Normal source checks: pin ancestor/monotonic/uniformity checker with real origin/main; both normal Cargo updates/fetches; fmt; ordinary workspace all-target Clippy; existing DBT-budget wrapper controls; measured prepared inventory through the actual producer path. Use owned bounded preparation scopes4CPU/8GiB/swap0/1024tasks/900s and genuine setup fetch bounds; report any real insufficiency rather than silently raise. The official closure owns its original per-node budgets, including64GiB build caps and7GiB Hermit-unit cap; a4CPU bootstrap is not a sibling-driver cap.

The official argv is with-proxy ./scripts/validate.rs full --allow-local-off-the-record-run --selected test.hermit_unit -j1 --no-label-pr --verbose --run-timeout10800, from the new clean committed source. Reuse the proven observer/controller3 mechanics, changing only explicit source/main/image/owner/output/cache identities and honest cache provenance. Normal external-cache parent ci-hub --help and validator --help preparation must be terminal. All current source entries/modes/gitlinks, current630 prepared identities, unchanged eleven rows and image bytes must be bound; do not hard-code old entry counts. Actual fresh main admission remains unchanged. No synthetic DAGRUN lineage, expected-count override, only-node bypass, environment fix, tracer or standalone Hermit execution.

Before guest launch send exact commit/tree/diff, all-source readback, preparation actuals, controller/config hashes and new absolute FULL.log destination to root. One separately released attempt only. Report bootstrap and authenticated real sibling bindings separately, preserve raw memory/pids events and actual statuses, and verify source plus all owned process generations/unit/cgroup terminal state. Native ten-test success is component evidence only; original Hermit DBT success, canonical main green and full backend assurance remain pending.
