The Reverie pin range d87a03a312421d34dee81dae71aa395b40231e63 → reviewed repair066a includes exactly one intervening commit, a28de6f8eb585d7095fa80e5f4c0fc41ec3ba9e4, Add mapped streams for blocking and asynchronous RPC. This is behavior attribution for the planned pin, not a fresh full correctness review of all mapped-stream implementation.

The five modified pre-existing files were read completely as a diff with their real callers. New mapped streams, serve_stream, from_connected_stream and try_send_rpc_with_io_scope are additive opt-in APIs. A whole tracked Rust search of Hermit2a414 and of Reveriea28 outside reverie-rpc-transport found no callers of those new APIs. BlockingRpcClient's default remains UnixStream; connect delegates to the same blocking handshake/framing implementation. No mapped transport is automatically selected by this pin. libc is added as a transport crate dependency.

There IS a carried production behavior change in reverie-rpc-transport/src/codec.rs:95–109. The async reader now distinguishes empty header EOF (RpcError::Closed) from EOF after 1–3 header bytes (RpcError::Io(UnexpectedEof)), and explicitly retries Interrupted reads. Previously read_exact UnexpectedEof became Closed even after a partial header. The framing length limit, complete payload read and bincode decoding stay unchanged.

This reader is used by existing Unix RpcClient handshake/responses and RpcServer request loops. server.rs:326 maps Closed to successful logical completion but propagates Io. In RpcServer::serve, the connection task logs that error at219–223 and retires its connection guard; it does NOT kill the listening server or manufacture a new guest exit. serve_one/serve_connection callers instead receive the error. Actual current Hermit SaBRe run_sabre creates RpcServer::bind_with_readiness at hermit-cli/src/lib.rs:1592 and runs serve. The pinned Reverie DBT launcher binds this same server at launcher.rs:569, then serve_rpc_until runs server.serve at1049. LiteInst backend also uses RpcServer and its blocking guest client remains the socket default. Therefore this is a stricter error classification on existing production malformed/truncated-frame paths, not merely unused APIs and not evidence of a normal guest behavior difference.

The carried tests partial_headers_are_failures_and_empty_eof_remains_closed and mapped_partial_request_is_not_a_successful_server_completion in tests/mapped_rpc.rs exercise zero/partial header distinctions. They have been read; this plan has not executed them. A focused source-bound execution of the existing partial-header methods can accompany pin preparation, preserving original expectations. No extra new test or scheduler change is proposed.

Goalpost assessment for this attribution: partial-header acceptance is tightened, not weakened; no prior assertion/tolerance/skip/comparator is changed to earn this pin. Clean EOF and complete-frame behavior remain. New API lifecycle limits are not recertified here. The native DR old-red/new-green fixture is independent of this RPC Rust crate, so it isolates the loader repair; future Hermit validation carries BOTH commits and must disclose both.
