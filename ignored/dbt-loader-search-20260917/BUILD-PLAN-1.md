# Genuine candidate native build

Base a28de6f8eb585d7095fa80e5f4c0fc41ec3ba9e4/tree38ace04089e4c8fdeb2d70daec5fa4a0478a698c. One product file currently changed: reverie-dbt/vendor/dynamorio/core/unix/loader.c. Patch candidate-1.patch SHA46f9de5f164a83cadf863df88a618e5bd4553401d3ea8f78b64bf49bc864b0ed; source5e444f19b7d4c474120c83cea03d15124d01766e79b8c0b7741131606f283f59.

The new fallback executes only after all six old lookup stages fail. It considers only top-level clients already carrying parsed dynamic metadata, and only a missing SONAME also directly listed in that client's DT_NEEDED. It searches the existing RUNPATH parser with global promotion disabled. The two original calls retain promotion=true, preserving their old behavior. No environment, path precedence, system fallback, loading/relocation order or error text is changed. MacOS retains a false fallback. Multiple eligible clients are visited in the already-existing locked module-list order; that affects only previously missing lookups. This is narrower than globally promoting client RUNPATH, and does not grant arbitrary indirect dependencies a new path.

Actual baseline is recorded outside the source slot at parent ignored/ci-hub/run1828-fix-forward-20260916/dbt-loader-search-1/native-fixture-design/controls-scope-1/results.json. All13 invocations retain their true status. Positive direct graph native0/107007 versus old DR255/missing leaf; ordered RUNPATH native0/107007 versus DR255. Existing successful DR choices remain211011(LD),111011(client dir),107007(RPATH). Missing and indirect-only refuse native1/DR255. No timeout/cap or resource event occurred. Those failed executions are not called passed guest executions.

The normal Reverie repository intentionally ignores Cargo.lock and has no tracked lock at this clean base; its CI uses cargo build --workspace --all-features. This focused build will first generate a real local ignored lock with cargo generate-lockfile --offline, record its bytes, then build with cargo build --offline --locked -p reverie-dbt -j4. No copied or fabricated lock/source-cache authority. If the private input cache lacks a required crate/index record, preserve that precise precondition and stop; any normal proxied fetch can be planned from the actual missing record.

Working directory: /home/newton/work/dev-hermit/worktrees/slots/dev-hermit-dbt-loader-search-20260917.
Private outputs: ignored/dbt-loader-search-20260917/native-build-1/{cargo,target,logs}. CARGO_HOME and CARGO_TARGET_DIR name those absolute paths. CARGO_BUILD_JOBS=4; no CI/build-ratchet/CMAKE/loader override. The installed nightly-2026-07-29 selected by the unchanged rust-toolchain.toml is used normally (versions in CANDIDATE-1.json).

Input seed: terminal6108 owned split/cargo, measured1,006,131,444 logical bytes. Copy only into the new private cargo destination with cp -a --reflink=auto; retain recursive path/type/bytes/mode/size/hash before and after, and require no shared writable file inode. Donor is never passed writable to Cargo. Generated target begins absent. Preserve old DR install and all original controls unchanged.

One owned observed scope4CPU/8GiB/0swap/1024tasks/900s covers preparation and real Cargo build. Retain exact argv/env public fields, raw memory/cpu/pids resource values, unit/InvocationID/cgroup inode and controller generation, actual exits and final absence. No whole-workspace or Hermit execution. build.rs must produce its own changed source-recipe key, actual full native CMake configure/build/install and normal attestation. Compare the new key to oldc9c1ee55; never edit the authority manifest or select the old install as candidate. This normal build also compiles the changed loader in release mode rather than merely parsing it.

After native build, run the same thirteen fixed native/DR invocations against the exact new bundled install in a separate2CPU/2GiB/256tasks/180s scope with10+2s per case and the original frozen ELF fixtures/expected values (only the two formerly missing DIRECT cases change expectation to successful107007). Keep indirect-only and physically missing refusals plus all existing precedence outcomes. Add an actual mixed direct-fallback-plus-unrelated-indirect control and an $ORIGIN path control if needed to verify the no-promotion branch; these are targeted new negative/path checks, not a relaxed oracle. No actual candidate DR run is included in this build plan's current pending release.

Durable regression will be a normal, non-ignored Linux x86_64 integration test in reverie-dbt/tests using the bundled_drrun_path and the five tiny C/assembly fixtures; it will use the same real ELF dependency assertions/marker values and ordinary timeout process-group wrapper already used in this repository. No prepared-cache override, copied implementation or success mock. Exact test diff and normal targeted test/Clippy plan will follow the source-level native result for independent review. No publication until review; Reverie main must land before any Hermit revision pin changes.
