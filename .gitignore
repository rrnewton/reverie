target/
Cargo.lock

# Machine-local validate-run ledger and other ignored runtime artifacts written
# under a nested dev-hermit slot (never committed).
ignored/

# Local backup and Python cache files.
*.orig
**/__pycache__/
**/.mypy_cache/

# reverie-dbi build.rs advisory lock guarding shared DynamoRIO source population.
.dynamorio-source.lock
