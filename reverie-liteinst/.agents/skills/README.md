# Codex skill entrypoints

Stock Codex discovers the LiteInst-specific Reverie skills here. Canonical
instruction bodies remain under `.llms/skills/` for Claude compatibility.

Run the repository-root `scripts/check-skill-discovery.rs` after changes.
